[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["custom_components/tibber_vehicle"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
